{
  "name": "@kiberos/openai-compat-proxy",
  "version": "0.1.0",
  "description": "Тонкий OpenAI→OpenAI pipe для любого OpenAI-совместимого апстрима (Qwen, DeepSeek, …). Апстрим — конфиг/заголовок, провайдер — не код.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2"
  }
}