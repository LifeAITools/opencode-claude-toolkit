{
  "name": "@kiberos/proxy-core",
  "version": "0.1.0",
  "description": "Переиспользуемое ядро тонких прокси: stats-emitter, sse-usage-tee, launch, health, jsonl-logger. Без провайдерной специфики.",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./launch": "./dist/launch.js"
  },
  "files": ["dist", "src/**/*.ts"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2"
  }
}